# Deprecated

Use **Vulkan SDK `glslc`** via `scripts/compile-shaders.ps1` instead.

This folder previously wrapped Glslang.NET as a fallback when `VULKAN_SDK` was unset. That produced stale or mismatched SPIR-V and is no longer used.
